import GLOBAL_CONSTANTS from '@/global_constants'
import React from 'react'

const WebViewWindow = ({ url, port }) => {
  return (
    <div>
        <h3>WebView Example</h3>
        {/* <div dangerouslySetInnerHTML={} /> */}
    </div>
  )
}

export default WebViewWindow